
    ABOUT:
        Header only single payload vulkan library
        written in C with a 64 character column limit.


    Where I got glslangValidator.exe :

        Distribution URL:

            https://github.com/KhronosGroup/glslang
            /releases/tag/master-tot

        Click On Asset Link:

            glslang-master-windows-x64-Debug.zip